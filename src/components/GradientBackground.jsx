const GradientBackground = () => {
    return (
        <div className="fixed inset-0 -z-10 bg-gradient-to-b from-custom-primary via-custom-background to-black" />
    );
};

export default GradientBackground;
